{
  "name": "@eslint-calibrate/eslint-plugin",
  "version": "0.1.12",
  "description": "ESLint Calibrate Plugin",
  "main": "index.js",
  "engines": {
    "node": ">=14.0.0"
  },
  "license": "ISC",
  "devDependencies": {
    "@eslint-calibrate/eslint-config": "^0.1.12",
    "eslint": "^8.8.0"
  },
  "peerDependencies": {
    "@eslint-calibrate/eslint-config": "^0.1.12",
    "eslint": ">=8.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
